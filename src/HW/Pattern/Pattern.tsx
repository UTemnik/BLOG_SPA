import React from 'react'
import s from './pattern.less'

const Pattern = () => {
     return (
         <div className={s.messages}>
            messages
         </div>
     )
}

export default Pattern;